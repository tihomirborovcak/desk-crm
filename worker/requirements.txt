# Transcription Worker dependencies
# Install: pip install -r requirements.txt

# HTTP requests
requests>=2.28.0

# PyTorch with CUDA support (za NVIDIA GPU)
# Instaliraj: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch
torchaudio

# Faster-whisper - brza transkripcija s GPU ubrzanjem
faster-whisper>=1.0.0
