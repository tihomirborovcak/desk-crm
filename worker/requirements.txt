# Transcription Worker dependencies
# Install: pip install -r requirements.txt

# HTTP requests
requests>=2.28.0

# PyTorch with CUDA support (za NVIDIA GPU)
# Instaliraj ručno: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
torch
torchaudio

# WhisperX - najbolja kvaliteta s word-level alignment
whisperx

# Faster-whisper kao fallback
faster-whisper>=0.9.0
